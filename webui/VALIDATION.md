# Web UI 验证记录（2026-09-09）

环境：Windows，Python 标准库 HTTP 服务，浏览器访问 `127.0.0.1:8765`。

- Python 编译、JavaScript 语法检查、git diff 空白检查通过。
- 浏览器确认总览排版正常；搜索 figure 正确筛出图件文档；弹窗展示真实原文。
- 浏览器按钮成功启动期刊校验；API 验证七项校验执行与结果返回。
- API 拒绝读取清单以外的 ../README.md 与 .git/config。

| 既有校验器 | 本次结果 |
| --- | --- |
| 跨 Skill 合同演练 | 失败，依赖的状态自测失败 |
| 流水线状态自测 | 失败，缺失锁定术语文件的预期错误断言不匹配 |
| 期刊决策/契约自测 | 通过 |
| 翻译交接自测 | 通过，31 项 |
| 译文审计自测 | 通过 |
| 文档结构审计自测 | 通过 |
| 参考资源检查 | 通过，37 个来源、127 个术语 |

状态校验器实际返回了术语文件缺失错误，但自测仍判失败；尚未修复根因。本次没有修改既有校验器。以上自测和合成演练不构成真实论文端到端验收。
